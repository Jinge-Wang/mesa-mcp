"""mesa-mcp: an MCP server that drives a local MESA installation.

The server stays a small set of deterministic tools; domain guidance lives in
`skills/mesa-agent/` (runtime) and `docs/development/` (development). See AGENTS.md.
"""

__version__ = "0.1.0"
